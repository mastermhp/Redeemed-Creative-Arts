/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ["placeholder.com", "images.unsplash.com", "v0.blob.com"],
    unoptimized: true,
  },
};

export default nextConfig;
